    // Todo: add business logic. 在这前后代码为框架所生成

    /* [NAPI_GEN]: function return value*/ 
[return_replace]
    OH_LOG_LOGINFOS(LOG_APP, "[funcName] get return info ends");
    OH_LOG_LOGINFOS(LOG_APP, "[funcName] ends");
    return [return_name]Out;
