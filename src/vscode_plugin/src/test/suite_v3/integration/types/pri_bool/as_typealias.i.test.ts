/* Auto-generated by suite_v3/tools/generate.ts — do not edit */
import * as assert from 'assert';
import { doParseTs } from '../../../../../parse/parsets';
import { transTskey2Ckey } from '../../../../../gen/gendts';

describe('SuiteV3_I_pri_bool_as_typealias', function () {
  this.timeout(30000);

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_RAW_NORM_000169
   * @tc.name testInteg_PRI_bool_TAL_RAW_NORM_000169
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.RAW.NORM.V0 input="bool" expect="boolean"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.RAW.NORM.V0
   */
  it('testInteg_PRI_bool_TAL_RAW_NORM_000169', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "bool";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "boolean", "G PRI_bool.TAL.RAW.NORM.V0 input=bool");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = boolean;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_RAW_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.RAW.NORM.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.RAW.NORM.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("bool");
    assert.strictEqual(mapped, "boolean", "I map==expect PRI_bool.TAL.RAW.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_RAW_BOUND_000170
   * @tc.name testInteg_PRI_bool_TAL_RAW_BOUND_000170
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.RAW.BOUND.V0 input="bool" expect="boolean"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.RAW.BOUND.V0
   */
  it('testInteg_PRI_bool_TAL_RAW_BOUND_000170', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "bool";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "boolean", "G PRI_bool.TAL.RAW.BOUND.V0 input=bool");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = boolean;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_RAW_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.RAW.BOUND.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.RAW.BOUND.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("bool");
    assert.strictEqual(mapped, "boolean", "I map==expect PRI_bool.TAL.RAW.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_RAW_ERR_000171
   * @tc.name testInteg_PRI_bool_TAL_RAW_ERR_000171
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.RAW.ERR.V0 input="__INVALID_TYPE_TOKEN_3029738__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair P+G-PRI_bool.TAL.RAW.ERR.V0
   */
  it('testInteg_PRI_bool_TAL_RAW_ERR_000171', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "__INVALID_TYPE_TOKEN_3029738__";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 负向期望 any
    assert.strictEqual(actual, 'any', "G negative PRI_bool.TAL.RAW.ERR.V0");
    // [P]
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_RAW_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_bool.TAL.RAW.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_RAW_SPEC_000172
   * @tc.name testInteg_PRI_bool_TAL_RAW_SPEC_000172
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.RAW.SPEC.V0 input="bool" expect="boolean"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.RAW.SPEC.V0
   */
  it('testInteg_PRI_bool_TAL_RAW_SPEC_000172', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "bool";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "boolean", "G PRI_bool.TAL.RAW.SPEC.V0 input=bool");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = boolean;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_RAW_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.RAW.SPEC.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.RAW.SPEC.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("bool");
    assert.strictEqual(mapped, "boolean", "I map==expect PRI_bool.TAL.RAW.SPEC.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_ARR1_NORM_000173
   * @tc.name testInteg_PRI_bool_TAL_ARR1_NORM_000173
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.ARR1.NORM.V0 input="std::vector<bool>" expect="Array<boolean>"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.ARR1.NORM.V0
   */
  it('testInteg_PRI_bool_TAL_ARR1_NORM_000173', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "std::vector<bool>";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "Array<boolean>", "G PRI_bool.TAL.ARR1.NORM.V0 input=std::vector<bool>");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = Array<boolean>;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_ARR1_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.ARR1.NORM.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.ARR1.NORM.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("std::vector<bool>");
    assert.strictEqual(mapped, "Array<boolean>", "I map==expect PRI_bool.TAL.ARR1.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_ARR1_BOUND_000174
   * @tc.name testInteg_PRI_bool_TAL_ARR1_BOUND_000174
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.ARR1.BOUND.V0 input="std::vector<bool>" expect="Array<boolean>"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.ARR1.BOUND.V0
   */
  it('testInteg_PRI_bool_TAL_ARR1_BOUND_000174', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "std::vector<bool>";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "Array<boolean>", "G PRI_bool.TAL.ARR1.BOUND.V0 input=std::vector<bool>");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = Array<boolean>;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_ARR1_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.ARR1.BOUND.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.ARR1.BOUND.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("std::vector<bool>");
    assert.strictEqual(mapped, "Array<boolean>", "I map==expect PRI_bool.TAL.ARR1.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_ARR1_ERR_000175
   * @tc.name testInteg_PRI_bool_TAL_ARR1_ERR_000175
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.ARR1.ERR.V0 input="__INVALID_TYPE_TOKEN_1196001538__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair P+G-PRI_bool.TAL.ARR1.ERR.V0
   */
  it('testInteg_PRI_bool_TAL_ARR1_ERR_000175', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "__INVALID_TYPE_TOKEN_1196001538__";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 负向期望 any
    assert.strictEqual(actual, 'any', "G negative PRI_bool.TAL.ARR1.ERR.V0");
    // [P]
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_ARR1_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_bool.TAL.ARR1.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_ARR1_SPEC_000176
   * @tc.name testInteg_PRI_bool_TAL_ARR1_SPEC_000176
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.ARR1.SPEC.V0 input="std::vector< bool >" expect="Array<boolean>"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.ARR1.SPEC.V0
   */
  it('testInteg_PRI_bool_TAL_ARR1_SPEC_000176', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "std::vector< bool >";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "Array<boolean>", "G PRI_bool.TAL.ARR1.SPEC.V0 input=std::vector< bool >");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = Array<boolean>;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_ARR1_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.ARR1.SPEC.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.ARR1.SPEC.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("std::vector< bool >");
    assert.strictEqual(mapped, "Array<boolean>", "I map==expect PRI_bool.TAL.ARR1.SPEC.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_NULLU_NORM_000177
   * @tc.name testInteg_PRI_bool_TAL_NULLU_NORM_000177
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.NULLU.NORM.V0 input="bool" expect="boolean"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.NULLU.NORM.V0
   */
  it('testInteg_PRI_bool_TAL_NULLU_NORM_000177', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "bool";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "boolean", "G PRI_bool.TAL.NULLU.NORM.V0 input=bool");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = boolean;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_NULLU_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.NULLU.NORM.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.NULLU.NORM.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("bool");
    assert.strictEqual(mapped, "boolean", "I map==expect PRI_bool.TAL.NULLU.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_NULLU_BOUND_000178
   * @tc.name testInteg_PRI_bool_TAL_NULLU_BOUND_000178
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.NULLU.BOUND.V0 input="bool" expect="boolean"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.NULLU.BOUND.V0
   */
  it('testInteg_PRI_bool_TAL_NULLU_BOUND_000178', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "bool";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "boolean", "G PRI_bool.TAL.NULLU.BOUND.V0 input=bool");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = boolean;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_NULLU_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.NULLU.BOUND.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.NULLU.BOUND.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("bool");
    assert.strictEqual(mapped, "boolean", "I map==expect PRI_bool.TAL.NULLU.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_NULLU_ERR_000179
   * @tc.name testInteg_PRI_bool_TAL_NULLU_ERR_000179
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.NULLU.ERR.V0 input="__INVALID_TYPE_TOKEN_3029738__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair P+G-PRI_bool.TAL.NULLU.ERR.V0
   */
  it('testInteg_PRI_bool_TAL_NULLU_ERR_000179', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "__INVALID_TYPE_TOKEN_3029738__";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 负向期望 any
    assert.strictEqual(actual, 'any', "G negative PRI_bool.TAL.NULLU.ERR.V0");
    // [P]
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_NULLU_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_bool.TAL.NULLU.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_NULLU_SPEC_000180
   * @tc.name testInteg_PRI_bool_TAL_NULLU_SPEC_000180
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.NULLU.SPEC.V0 input="bool" expect="boolean"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.NULLU.SPEC.V0
   */
  it('testInteg_PRI_bool_TAL_NULLU_SPEC_000180', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "bool";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "boolean", "G PRI_bool.TAL.NULLU.SPEC.V0 input=bool");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = boolean;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_NULLU_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.NULLU.SPEC.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.NULLU.SPEC.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("bool");
    assert.strictEqual(mapped, "boolean", "I map==expect PRI_bool.TAL.NULLU.SPEC.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_PROM_NORM_000181
   * @tc.name testInteg_PRI_bool_TAL_PROM_NORM_000181
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.PROM.NORM.V0 input="bool" expect="boolean"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.PROM.NORM.V0
   */
  it('testInteg_PRI_bool_TAL_PROM_NORM_000181', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "bool";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "boolean", "G PRI_bool.TAL.PROM.NORM.V0 input=bool");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = boolean;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_PROM_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.PROM.NORM.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.PROM.NORM.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("bool");
    assert.strictEqual(mapped, "boolean", "I map==expect PRI_bool.TAL.PROM.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_PROM_BOUND_000182
   * @tc.name testInteg_PRI_bool_TAL_PROM_BOUND_000182
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.PROM.BOUND.V0 input="bool" expect="boolean"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.PROM.BOUND.V0
   */
  it('testInteg_PRI_bool_TAL_PROM_BOUND_000182', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "bool";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "boolean", "G PRI_bool.TAL.PROM.BOUND.V0 input=bool");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = boolean;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_PROM_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.PROM.BOUND.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.PROM.BOUND.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("bool");
    assert.strictEqual(mapped, "boolean", "I map==expect PRI_bool.TAL.PROM.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_PROM_ERR_000183
   * @tc.name testInteg_PRI_bool_TAL_PROM_ERR_000183
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.PROM.ERR.V0 input="__INVALID_TYPE_TOKEN_3029738__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair P+G-PRI_bool.TAL.PROM.ERR.V0
   */
  it('testInteg_PRI_bool_TAL_PROM_ERR_000183', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "__INVALID_TYPE_TOKEN_3029738__";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 负向期望 any
    assert.strictEqual(actual, 'any', "G negative PRI_bool.TAL.PROM.ERR.V0");
    // [P]
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_PROM_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_bool.TAL.PROM.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_INTEG_PRI_bool_TAL_PROM_SPEC_000184
   * @tc.name testInteg_PRI_bool_TAL_PROM_SPEC_000184
   * @tc.desc Verify roundtrip parse+map for PRI_bool.TAL.PROM.SPEC.V0 input="bool" expect="boolean"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair P+G-PRI_bool.TAL.PROM.SPEC.V0
   */
  it('testInteg_PRI_bool_TAL_PROM_SPEC_000184', function () {
    // --- 集成：先映射，再解析，再交叉校验 ---
    // [G]
    // step1: 输入 C/声明类型
    const inputType = "bool";
    // step2: 映射为 TS 类型
    const actual = transTskey2Ckey(inputType);
    // step3: 断言映射结果
    assert.strictEqual(actual, "boolean", "G PRI_bool.TAL.PROM.SPEC.V0 input=bool");
    // [P]
    // step1: 构造待解析源码
    const src = "export type NapiV2Alias = boolean;\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_bool_TAL_PROM_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_bool.TAL.PROM.SPEC.V0 parse null");
    // step4: 校验 type alias 存在
    assert.ok(res.types && res.types.length >= 1, "P TAL PRI_bool.TAL.PROM.SPEC.V0");
    // [I] map 与 expect 一致
    const mapped = transTskey2Ckey("bool");
    assert.strictEqual(mapped, "boolean", "I map==expect PRI_bool.TAL.PROM.SPEC.V0");
  });

});
