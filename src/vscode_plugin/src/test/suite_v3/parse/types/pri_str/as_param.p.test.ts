/* Auto-generated by suite_v3/tools/generate.ts — do not edit */
import * as assert from 'assert';
import { doParseTs } from '../../../../../parse/parsets';

describe('SuiteV3_P_pri_str_as_param', function () {
  this.timeout(30000);

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_RAW_NORM_000225
   * @tc.name testParse_PRI_str_PAR_RAW_NORM_000225
   * @tc.desc Verify parse declaration for PRI_str.PAR.RAW.NORM.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.RAW.NORM.V0
   */
  it('testParse_PRI_str_PAR_RAW_NORM_000225', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_RAW_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.RAW.NORM.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.RAW.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_RAW_BOUND_000226
   * @tc.name testParse_PRI_str_PAR_RAW_BOUND_000226
   * @tc.desc Verify parse declaration for PRI_str.PAR.RAW.BOUND.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.RAW.BOUND.V0
   */
  it('testParse_PRI_str_PAR_RAW_BOUND_000226', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_RAW_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.RAW.BOUND.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.RAW.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_RAW_ERR_000227
   * @tc.name testParse_PRI_str_PAR_RAW_ERR_000227
   * @tc.desc Verify parse declaration for PRI_str.PAR.RAW.ERR.V0 input="__INVALID_TYPE_TOKEN_891985903__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair G-PRI_str.PAR.RAW.ERR.V0
   */
  it('testParse_PRI_str_PAR_RAW_ERR_000227', function () {
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_RAW_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_str.PAR.RAW.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_RAW_SPEC_000228
   * @tc.name testParse_PRI_str_PAR_RAW_SPEC_000228
   * @tc.desc Verify parse declaration for PRI_str.PAR.RAW.SPEC.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.RAW.SPEC.V0
   */
  it('testParse_PRI_str_PAR_RAW_SPEC_000228', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_RAW_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.RAW.SPEC.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.RAW.SPEC.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_OPT_NORM_000229
   * @tc.name testParse_PRI_str_PAR_OPT_NORM_000229
   * @tc.desc Verify parse declaration for PRI_str.PAR.OPT.NORM.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.OPT.NORM.V0
   */
  it('testParse_PRI_str_PAR_OPT_NORM_000229', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_OPT_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.OPT.NORM.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.OPT.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_OPT_BOUND_000230
   * @tc.name testParse_PRI_str_PAR_OPT_BOUND_000230
   * @tc.desc Verify parse declaration for PRI_str.PAR.OPT.BOUND.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.OPT.BOUND.V0
   */
  it('testParse_PRI_str_PAR_OPT_BOUND_000230', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_OPT_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.OPT.BOUND.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.OPT.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_OPT_ERR_000231
   * @tc.name testParse_PRI_str_PAR_OPT_ERR_000231
   * @tc.desc Verify parse declaration for PRI_str.PAR.OPT.ERR.V0 input="__INVALID_TYPE_TOKEN_891985903__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair G-PRI_str.PAR.OPT.ERR.V0
   */
  it('testParse_PRI_str_PAR_OPT_ERR_000231', function () {
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_OPT_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_str.PAR.OPT.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_OPT_SPEC_000232
   * @tc.name testParse_PRI_str_PAR_OPT_SPEC_000232
   * @tc.desc Verify parse declaration for PRI_str.PAR.OPT.SPEC.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.OPT.SPEC.V0
   */
  it('testParse_PRI_str_PAR_OPT_SPEC_000232', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_OPT_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.OPT.SPEC.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.OPT.SPEC.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_RO_NORM_000233
   * @tc.name testParse_PRI_str_PAR_RO_NORM_000233
   * @tc.desc Verify parse declaration for PRI_str.PAR.RO.NORM.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.RO.NORM.V0
   */
  it('testParse_PRI_str_PAR_RO_NORM_000233', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_RO_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.RO.NORM.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.RO.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_RO_BOUND_000234
   * @tc.name testParse_PRI_str_PAR_RO_BOUND_000234
   * @tc.desc Verify parse declaration for PRI_str.PAR.RO.BOUND.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.RO.BOUND.V0
   */
  it('testParse_PRI_str_PAR_RO_BOUND_000234', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_RO_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.RO.BOUND.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.RO.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_RO_ERR_000235
   * @tc.name testParse_PRI_str_PAR_RO_ERR_000235
   * @tc.desc Verify parse declaration for PRI_str.PAR.RO.ERR.V0 input="__INVALID_TYPE_TOKEN_891985903__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair G-PRI_str.PAR.RO.ERR.V0
   */
  it('testParse_PRI_str_PAR_RO_ERR_000235', function () {
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_RO_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_str.PAR.RO.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_RO_SPEC_000236
   * @tc.name testParse_PRI_str_PAR_RO_SPEC_000236
   * @tc.desc Verify parse declaration for PRI_str.PAR.RO.SPEC.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.RO.SPEC.V0
   */
  it('testParse_PRI_str_PAR_RO_SPEC_000236', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_RO_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.RO.SPEC.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.RO.SPEC.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_PTR_NORM_000237
   * @tc.name testParse_PRI_str_PAR_PTR_NORM_000237
   * @tc.desc Verify parse declaration for PRI_str.PAR.PTR.NORM.V0 input="string*" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.PTR.NORM.V0
   */
  it('testParse_PRI_str_PAR_PTR_NORM_000237', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_PTR_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.PTR.NORM.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.PTR.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_PTR_BOUND_000238
   * @tc.name testParse_PRI_str_PAR_PTR_BOUND_000238
   * @tc.desc Verify parse declaration for PRI_str.PAR.PTR.BOUND.V0 input="string *" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.PTR.BOUND.V0
   */
  it('testParse_PRI_str_PAR_PTR_BOUND_000238', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_PTR_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.PTR.BOUND.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.PTR.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_PTR_ERR_000239
   * @tc.name testParse_PRI_str_PAR_PTR_ERR_000239
   * @tc.desc Verify parse declaration for PRI_str.PAR.PTR.ERR.V0 input="__INVALID_TYPE_TOKEN_891985903__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair G-PRI_str.PAR.PTR.ERR.V0
   */
  it('testParse_PRI_str_PAR_PTR_ERR_000239', function () {
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_PTR_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_str.PAR.PTR.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_PTR_SPEC_000240
   * @tc.name testParse_PRI_str_PAR_PTR_SPEC_000240
   * @tc.desc Verify parse declaration for PRI_str.PAR.PTR.SPEC.V0 input="string*" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.PTR.SPEC.V0
   */
  it('testParse_PRI_str_PAR_PTR_SPEC_000240', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_PTR_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.PTR.SPEC.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.PTR.SPEC.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_ARR1_NORM_000241
   * @tc.name testParse_PRI_str_PAR_ARR1_NORM_000241
   * @tc.desc Verify parse declaration for PRI_str.PAR.ARR1.NORM.V0 input="std::vector<string>" expect="Array<string>"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.ARR1.NORM.V0
   */
  it('testParse_PRI_str_PAR_ARR1_NORM_000241', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: Array<string>): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_ARR1_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.ARR1.NORM.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.ARR1.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_ARR1_BOUND_000242
   * @tc.name testParse_PRI_str_PAR_ARR1_BOUND_000242
   * @tc.desc Verify parse declaration for PRI_str.PAR.ARR1.BOUND.V0 input="std::vector<string>" expect="Array<string>"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.ARR1.BOUND.V0
   */
  it('testParse_PRI_str_PAR_ARR1_BOUND_000242', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: Array<string>): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_ARR1_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.ARR1.BOUND.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.ARR1.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_ARR1_ERR_000243
   * @tc.name testParse_PRI_str_PAR_ARR1_ERR_000243
   * @tc.desc Verify parse declaration for PRI_str.PAR.ARR1.ERR.V0 input="__INVALID_TYPE_TOKEN_252672969__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair G-PRI_str.PAR.ARR1.ERR.V0
   */
  it('testParse_PRI_str_PAR_ARR1_ERR_000243', function () {
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_ARR1_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_str.PAR.ARR1.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_ARR1_SPEC_000244
   * @tc.name testParse_PRI_str_PAR_ARR1_SPEC_000244
   * @tc.desc Verify parse declaration for PRI_str.PAR.ARR1.SPEC.V0 input="std::vector< string >" expect="Array<string>"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.ARR1.SPEC.V0
   */
  it('testParse_PRI_str_PAR_ARR1_SPEC_000244', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: Array<string>): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_ARR1_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.ARR1.SPEC.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.ARR1.SPEC.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_NULLU_NORM_000245
   * @tc.name testParse_PRI_str_PAR_NULLU_NORM_000245
   * @tc.desc Verify parse declaration for PRI_str.PAR.NULLU.NORM.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.NULLU.NORM.V0
   */
  it('testParse_PRI_str_PAR_NULLU_NORM_000245', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_NULLU_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.NULLU.NORM.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.NULLU.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_NULLU_BOUND_000246
   * @tc.name testParse_PRI_str_PAR_NULLU_BOUND_000246
   * @tc.desc Verify parse declaration for PRI_str.PAR.NULLU.BOUND.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.NULLU.BOUND.V0
   */
  it('testParse_PRI_str_PAR_NULLU_BOUND_000246', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_NULLU_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.NULLU.BOUND.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.NULLU.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_NULLU_ERR_000247
   * @tc.name testParse_PRI_str_PAR_NULLU_ERR_000247
   * @tc.desc Verify parse declaration for PRI_str.PAR.NULLU.ERR.V0 input="__INVALID_TYPE_TOKEN_891985903__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair G-PRI_str.PAR.NULLU.ERR.V0
   */
  it('testParse_PRI_str_PAR_NULLU_ERR_000247', function () {
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_NULLU_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_str.PAR.NULLU.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_NULLU_SPEC_000248
   * @tc.name testParse_PRI_str_PAR_NULLU_SPEC_000248
   * @tc.desc Verify parse declaration for PRI_str.PAR.NULLU.SPEC.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.NULLU.SPEC.V0
   */
  it('testParse_PRI_str_PAR_NULLU_SPEC_000248', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_NULLU_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.NULLU.SPEC.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.NULLU.SPEC.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_PROM_NORM_000249
   * @tc.name testParse_PRI_str_PAR_PROM_NORM_000249
   * @tc.desc Verify parse declaration for PRI_str.PAR.PROM.NORM.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.PROM.NORM.V0
   */
  it('testParse_PRI_str_PAR_PROM_NORM_000249', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_PROM_NORM_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.PROM.NORM.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.PROM.NORM.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_PROM_BOUND_000250
   * @tc.name testParse_PRI_str_PAR_PROM_BOUND_000250
   * @tc.desc Verify parse declaration for PRI_str.PAR.PROM.BOUND.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.PROM.BOUND.V0
   */
  it('testParse_PRI_str_PAR_PROM_BOUND_000250', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_PROM_BOUND_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.PROM.BOUND.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.PROM.BOUND.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_PROM_ERR_000251
   * @tc.name testParse_PRI_str_PAR_PROM_ERR_000251
   * @tc.desc Verify parse declaration for PRI_str.PAR.PROM.ERR.V0 input="__INVALID_TYPE_TOKEN_891985903__" expect="any"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 2
   * @pair G-PRI_str.PAR.PROM.ERR.V0
   */
  it('testParse_PRI_str_PAR_PROM_ERR_000251', function () {
    // step1: 构造待解析源码
    const src = "export class NapiV2Neg { v: any; }\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_PROM_ERR_V0.ts", src);
    // step3: 负向 — 解析不崩溃且返回 classes
    assert.ok(res && Array.isArray(res.classes), "P neg PRI_str.PAR.PROM.ERR.V0");
  });

  /**
   * @tc.number SUB_NAPI_PARSE_PRI_str_PAR_PROM_SPEC_000252
   * @tc.name testParse_PRI_str_PAR_PROM_SPEC_000252
   * @tc.desc Verify parse declaration for PRI_str.PAR.PROM.SPEC.V0 input="string" expect="string"
   * @tc.size MediumTest
   * @tc.type Function
   * @tc.level Level 1
   * @pair G-PRI_str.PAR.PROM.SPEC.V0
   */
  it('testParse_PRI_str_PAR_PROM_SPEC_000252', function () {
    // step1: 构造待解析源码
    const src = "export function napiV2Fn(p: string): void {}\n";
    // step2: 调用解析
    const res = doParseTs("v3_PRI_str_PAR_PROM_SPEC_V0.ts", src);
    // step3: 解析结果非空
    assert.ok(res, "P PRI_str.PAR.PROM.SPEC.V0 parse null");
    // step4: 校验函数声明存在
    assert.ok(res.funcs && res.funcs.length >= 1, "P func PRI_str.PAR.PROM.SPEC.V0");
  });

});
